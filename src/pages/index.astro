---
export const prerender = true;

import { getCollection } from 'astro:content';

import BaseLayout from '../layouts/BaseLayout.astro';

import CallToAction from '../components/CallToAction.astro';
import Grid from '../components/Grid.astro';
import Hero from '../components/Hero.astro';
import Icon from '../components/Icon.astro';
import Pill from '../components/Pill.astro';
import PortfolioPreview from '../components/PortfolioPreview.astro';

import ContactCTA from '../components/ContactCTA.astro';
import Skills from '../components/Skills.astro';
import PostPreview from '../components/PostPreview.astro';

import Portrait from '../assets/portrait.webp';

import { University } from '@lucide/astro';
import { getSiteStructuredData } from '../utils/seo';


const projects = (await getCollection('work'))
	.sort((a, b) => b.data.updatedDate.valueOf() - a.data.updatedDate.valueOf())
	.slice(0, 4);

const posts = (await getCollection('blog'))
	.sort((a, b) => b.data.updatedDate.valueOf() - a.data.updatedDate.valueOf())
	.slice(0, 4);

const site = Astro.site ?? new URL('https://www.dcesares.dev');
const structuredData = getSiteStructuredData(site);

---

<BaseLayout structuredData={structuredData}>
	<div class="stack gap-20 lg:gap-48">
		<div class="wrapper stack gap-8 lg:gap-20">
			<header class="hero" data-animate="fade-up">
				<Hero
					title="Isaac D'Césares"
					tagline="Traduzo tecnologias emergentes em experiências que transformam como pessoas e instituições aprendem."
					align="start"
				>
					<div class="roles" data-animate-stagger>
						<Pill color="terracotta"><Icon icon="book-open" size="1.33em" /> Pesquisador</Pill>
						<Pill color="sage"><University /> Educador</Pill>
						<Pill color="teal"><Icon icon="terminal-window" size="1.33em" /> Construtor</Pill>
					</div>

					<div class="hero-actions">
						<CallToAction href="/work/">
							O que venho construindo
							<Icon icon="arrow-right" size="1.2em" />
						</CallToAction>
						<a class="ghost-link" href="/about/">Quem sou e como penso</a>
					</div>

				</Hero>

				<img
					class="portrait"
					alt="Isaac D'Césares profile picture"
					src={Portrait.src}
					loading="eager"
					width="400"
					height="300"
					decoding="async"
					data-animate="scale-up"
					data-animate-delay="200"
				/>
			</header>

<!-- 			<section class="tese" data-animate="fade-up">
				<p>
					Existe uma membrana entre a pesquisa e a prática, entre o protótipo e a instituição. É aí que eu trabalho. Pego IA, blockchain e sistemas emergentes e traduzo em ferramentas que professores conseguem usar, estudantes reconhecem como suas e instituições conseguem sustentar.
				</p>
			</section> -->

			<Skills />
		</div>

		<main class="wrapper stack gap-20 lg:gap-48">
			<section class="section with-background with-cta" data-animate="fade-up">
				<header class="section-header stack gap-2 lg:gap-4">
					<h3>Últimos trabalhos</h3>
					<p>Registros recentes de entregas, projetos e colaborações.</p>
				</header>

				<div class="gallery" data-animate-stagger>
					<Grid variant="offset">
						{
							projects.map((project) => (
								<li data-animate="fade-up">
									<PortfolioPreview project={project} />
								</li>
							))
						}
					</Grid>
				</div>

				<div class="cta">
					<CallToAction href="/work/">
						Ver todos os trabalhos
						<Icon icon="arrow-right" size="1.2em" />
					</CallToAction>
				</div>
			</section>
		</main>

		<main class="wrapper stack gap-20 lg:gap-48">
			<section class="section with-background with-cta bg-variant" data-animate="fade-up">
				<header class="section-header stack gap-2 lg:gap-4">
					<h3>Últimas postagens</h3>
					<p>Notas técnicas e reflexões sobre educação, IA e o que tenho aprendido no caminho.</p>
				</header>

				<div class="gallery" data-animate-stagger>
					<Grid variant="offset">
						{
							posts.map((post) => (
								<li data-animate="fade-up">
									<PostPreview post={post} />
								</li>
							))
						}
					</Grid>
				</div>

				<div class="cta">
					<CallToAction href="/blog/">
						Ler todas as postagens
						<Icon icon="arrow-right" size="1.2em" />
					</CallToAction>
				</div>
			</section>
		</main>

		<ContactCTA />
	</div>
</BaseLayout>

<style>
	.hero {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: var(--space-8);
	}

	.roles {
		display: flex;
		flex-wrap: wrap;
		gap: var(--space-2);
	}

	.hero img {
		aspect-ratio: 5 / 4;
		object-fit: cover;
		object-position: top;
		border-radius: var(--radius-xl);
		box-shadow: var(--shadow-md);
		filter: var(--photo-warmth);
	}

	.hero-actions {
		display: flex;
		flex-wrap: wrap;
		gap: var(--space-3);
		align-items: center;
	}

	.ghost-link {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		padding: var(--space-3) var(--space-6);
		border-radius: var(--radius-full);
		border: 1px solid var(--color-border-default);
		color: var(--color-text-secondary);
		text-decoration: none;
		font-family: var(--font-sans);
		font-weight: var(--weight-medium);
		transition: all var(--transition-default);
		background: color-mix(in oklab, var(--color-bg-primary) 80%, transparent);
		box-shadow: var(--shadow-sm);
		min-height: 44px;
	}

	.ghost-link:hover {
		color: var(--color-text-primary);
		border-color: var(--color-border-emphasis);
		transform: translateY(-1px);
	}

	@media (min-width: 50em) {
		.hero {
			display: grid;
			grid-template-columns: 6fr 4fr;
			padding-inline: var(--space-10);
			gap: var(--space-16);
		}

		.roles {
			margin-top: var(--space-2);
			gap: var(--space-2);
		}

		.hero img {
			aspect-ratio: 3 / 4;
			border-radius: var(--radius-xl);
			object-fit: cover;
		}
	}

	/* ====================================================== */

	.tese {
		max-width: 60ch;
		margin-inline: auto;
		text-align: center;
	}

	.tese p {
		font-family: var(--font-serif);
		font-size: var(--text-h3);
		line-height: var(--leading-subheading);
		color: var(--color-text-primary);
		margin: 0;
	}

	@media (min-width: 50em) {
		.tese p {
			font-size: var(--text-h2);
			line-height: var(--leading-subheading);
		}
	}

	/* ====================================================== */

	.section {
		display: grid;
		gap: var(--space-8);
	}

	.with-background {
		position: relative;
	}

	.with-background::before {
		content: '';
		position: absolute;
		pointer-events: none;
		left: 50%;
		width: 100vw;
		height: 140%;
		top: -40%;
		transform: translateX(-50%);
		background:
			radial-gradient(800px 400px at 50% 10%, var(--color-terracotta-10), transparent 70%),
			radial-gradient(600px 300px at 80% 20%, var(--color-teal-08), transparent 65%);
		opacity: var(--opacity-heavy);
		z-index: -1;
	}

	.with-background.bg-variant::before {
		background:
			radial-gradient(800px 400px at 40% 10%, var(--color-teal-12), transparent 70%),
			radial-gradient(600px 300px at 70% 20%, var(--color-sage-10), transparent 65%);
	}

	.section-header {
		justify-self: center;
		text-align: center;
		max-width: 50ch;
		font-size: var(--text-body-lg);
		color: var(--color-text-secondary);
	}

	.section-header h3 {
		font-size: var(--text-h1);
		letter-spacing: var(--tracking-heading);
	}

	@media (min-width: 50em) {
		.section {
			grid-template-columns: repeat(4, 1fr);
			grid-template-areas: 'header header header header' 'gallery gallery gallery gallery';
			gap: var(--space-20);
		}

		.section.with-cta {
			grid-template-areas: 'header header header cta' 'gallery gallery gallery gallery';
		}

		.section-header {
			grid-area: header;
			font-size: var(--text-h3);
		}

		.section-header h3 {
			font-size: var(--text-display);
		}

		.with-cta .section-header {
			justify-self: flex-start;
			text-align: left;
		}

		.gallery {
			grid-area: gallery;
		}

		.cta {
			grid-area: cta;
		}
	}

	/* ====================================================== */

	.mention-card {
		display: flex;
		height: 7rem;
		justify-content: center;
		align-items: center;
		text-align: center;
		border: 1px solid var(--color-border-default);
		border-radius: var(--radius-xl);
		color: var(--color-text-secondary);
		background: var(--gradient-subtle);
		box-shadow: var(--shadow-sm);
	}

	@media (min-width: 50em) {
		.mention-card {
			border-radius: var(--radius-xl);
			height: 9.5rem;
		}
	}
</style>
